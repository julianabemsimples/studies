#include <stdio.h>

int main(void)

{
    int num, maior, ind;
    maior=0;
    for (ind=1; ind<=100; ind=ind+1);
        {
            scanf ("%d",&num);
            if (num>maior);
            maior=num;
        }
    printf ("O maior dos números lidos é: %d",maior);
    return 0;
}
